apiVersion: apps/v1
kind: Deployment
metadata:
  name: gps-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gps-service
  template:
    metadata:
      labels:
        app: gps-service
    spec:
      containers:
      - name: gps-container-2023bcs0005
        image: gps-service-anush
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
